# PRD v1.1 (Final)

## نظام مكة لإدارة محلات الذهب والفضة

| البيان | القيمة |
|---|---|
| نوع الوثيقة | Product Requirements Document |
| الإصدار | 1.1 |
| الحالة | **Final — Frozen** |
| تاريخ التجميد | 28 يوليو 2026 |
| اللغة | العربية، باتجاه RTL |
| نطاق المنتج | تطبيق ويب/PWA لإدارة محل ذهب وفضة في مصر |
| مالك القرار التجاري | صاحب المنتج |
| مالك القرار المحاسبي | المحاسب المعتمد للمنتج |

> هذه الوثيقة هي مصدر الحقيقة الوحيد لمتطلبات المنتج في نطاق v1.1.
>
> كلمة **يجب** تعني متطلبًا إلزاميًا. كلمة **مؤجل** تعني أنه خارج MVP ولا يجوز تفعيله بافتراضات محلية.

---

## 1. غرض الوثيقة وحدودها

تحدد هذه الوثيقة المنتج المطلوب، قواعده التجارية والمحاسبية، نطاق MVP، معايير القبول، والفجوات التي تمنع الإطلاق.

اعتماد الوثيقة كـ`Final` يعني أن المتطلبات حُسمت بما يكفي للتنفيذ والاختبار. لا يعني أن التطبيق الحالي مستوفٍ لكل المتطلبات أو جاهز للإنتاج قبل إغلاق Release Blockers الواردة في القسم 19.

### 1.1 ترتيب مصادر الحقيقة

عند وجود تعارض، يُطبق الترتيب التالي:

1. هذه الوثيقة بعد اعتمادها.
2. قواعد Firestore والأمن المنشورة.
3. الاختبارات المحاسبية وGolden Baseline المعتمد في المستودع.
4. التنفيذ الحالي.
5. وثائق التصميم والتدقيق المؤرخة.
6. وثائق الترحيل القديمة وREADME.

أي وثيقة تقترح `Balancing Plug`، تكلفة سوق افتراضية، أو تغييرًا صامتًا للتاريخ تُعد `Superseded` إذا خالفت هذه الوثيقة.

### 1.2 أوضاع النظام المعتمدة

| الوضع | الاستخدام المعتمد في v1.1 |
|---|---|
| `Legacy Ledger` | دفتر التشغيل الحالي لدفتر الأستاذ وميزان المراجعة |
| `Phase 5 Cost Engine` | المصدر الوحيد لتكلفة المخزون وCOGS والربح الإجمالي المحقق |
| `Canonical Accounting` | `Shadow/Preview` فقط؛ ليس دفتر إنتاج ولا مصدر تقرير رسمي |
| أسعار السوق | تقييم وعرض فقط؛ لا تدخل في التكلفة الدفترية |

لا يجوز دمج أرقام من هذه الأوضاع دون إظهار المصدر وإجراء مصالحة صريحة.

---

## 2. رؤية المنتج

تمكين صاحب محل الذهب من معرفة ما يلي بثقة وفي أي وقت:

- رصيد الخزنة بالجنيه المصري.
- المخزون الفعلي من الذهب والفضة والملحقات.
- التزامات التجار المعدنية.
- صافي ملكية المحل من المعدن.
- تكلفة المخزون والربح المحقق من المبيعات.
- مصدر كل رقم والعملية التي أنشأته.

### مبدأ المنتج

> كل عملية مفهومة تشغيليًا، صحيحة داخل أبعادها، قابلة للتتبع، وقابلة لإعادة الحساب بنفس النتيجة.

---

## 3. بيان المشكلة

برامج الحسابات العامة لا تمثل تجارة الذهب بدقة لأن العملية الواحدة قد تؤثر بالتوازي في:

- النقد بالجنيه.
- وزن الذهب بعيارات 18 و21 و24.
- وزن الفضة.
- عدد قطع الملحقات.
- تكلفة المخزون.
- التزام تاجر بمعدن وليس بقيمة نقدية فقط.

أي خلط بين هذه الأبعاد قد ينتج رصيدًا متوازنًا شكليًا لكنه خاطئ اقتصاديًا. كما أن البيانات التاريخية قد تحتوي على أسماء حسابات قديمة، ترتيب غير موثوق داخل اليوم، أو معرفات ناقصة؛ لذلك يجب الحفاظ على المصدر وعدم اختراع بيانات أو قيود لسد الفروق.

---

## 4. الأهداف

### 4.1 أهداف العمل

- تقليل أخطاء تسجيل النقد والوزن.
- إظهار السيولة والمخزون وصافي الملكية يوميًا.
- حساب COGS والربح الإجمالي من تكلفة قابلة للتدقيق.
- تسهيل الجرد ومعالجة الفروق المعتمدة.
- الحفاظ على التاريخ وأرقام المصدر دون إعادة ترقيم أو تعديل صامت.

### 4.2 أهداف المنتج

- مسار موحد لإنشاء القيود وتعديلها وحذفها وترحيل الجرد.
- منع أي كتابة تجعل المخزون أو محرك التكلفة غير صالح.
- مصدر واضح لكل تقرير.
- تجربة عربية، RTL، ومهيأة للهاتف.
- صلاحيات مبنية على الخادم وسجل تدقيق للعمليات الحساسة.

### 4.3 مؤشرات النجاح

- صفر قيود جديدة بلا معرف حساب ثابت.
- صفر كتابات حساسة بلا سجل تدقيق.
- صفر نتائج تكلفة جزئية عند فشل المحرك.
- تطابق الشاشة الرئيسية والتقارير عند استخدام نفس المصدر وتاريخ القطع.
- إمكانية تتبع كل إجمالي تقرير إلى القيود المكونة له.
- نجاح الاستعادة الدورية من النسخة الاحتياطية.

---

## 5. غير الأهداف

العناصر التالية ليست جزءًا من v1.1:

- محاسبة ضريبية أو إقرارات ضريبة قيمة مضافة.
- الفاتورة الإلكترونية المصرية.
- الربط البنكي أو نقاط البيع.
- التجارة الإلكترونية.
- الرواتب والموارد البشرية.
- الفروع أو الشركات المتعددة.
- تكامل آلي مع بورصة أو مزود أسعار.
- تقارير مالية قانونية أو مدققة.
- تفعيل Canonical كدفتر إنتاج.
- العمل الكامل دون اتصال.
- المرتجعات.
- صلاحيات موظف إدخال أو مراجع كأدوار مستقلة.
- استيراد عام من واجهة الإعدادات.
- إعادة ترقيم التاريخ أو حذف كل البيانات من الواجهة.

عدم وجود العنصر في `Non-Goals` لا يجعله ضمن النطاق تلقائيًا؛ نطاق MVP محدد حصريًا في القسم 17.

---

## 6. المستخدمون والصلاحيات

يدعم v1.1 دورين فقط لأنهما المثبتان في التنفيذ وقواعد البيانات:

| الدور | مصدر الصلاحية | النطاق |
|---|---|---|
| `user` | مستخدم Firebase Auth بلا Admin Claim | بياناته المملوكة فقط |
| `admin` | `request.auth.token.admin == true` | البيانات والنطاقات الإدارية المسموح بها في Firestore Rules |

### 6.1 قواعد الصلاحية

- البريد الموجود في `VITE_ADMIN_EMAIL` ليس مصدر صلاحية.
- حقل `role` داخل وثيقة المستخدم وصفي ولا يمنح صلاحية.
- لا يستطيع العميل منح أو رفع دور.
- إذا اختلف عرض الواجهة عن Firestore Rules، تكون قواعد الخادم هي الحكم.
- أي دور إضافي مؤجل إلى إصدار لاحق.

### 6.2 مصفوفة الصلاحيات

| الإجراء | user | admin |
|---|---:|---:|
| قراءة وكتابة قيوده الخاصة | نعم | نعم |
| قراءة قيود مستخدم آخر | لا | حسب قواعد النطاق الإداري |
| إدارة حساباته وقواعده الخاصة | نعم | نعم |
| إنشاء/تعديل جرد خاص به | نعم | نعم |
| تعديل جرد مرحل | لا | لا |
| قراءة سجل التدقيق | لا | نعم |
| تنفيذ ترحيل تاريخي معتمد | لا | نعم |
| حذف جماعي أو تغيير أدوار | لا | لا من واجهة v1.1 |
| تفعيل Canonical | لا | لا في v1.1 |

---

## 7. نطاق العمليات

### 7.1 العمليات المتاحة

تُتاح العملية فقط إذا وجد لها `Transaction Rule` صالح يحدد الحسابات المسموح بها وحقولها المطلوبة. تشمل العائلات الحالية:

- بيع وشراء الذهب.
- بيع وشراء الفضة.
- بيع وشراء الملحقات.
- إيرادات أخرى.
- قبض من عميل ودفع لعميل.
- معاملات تجار الذهب والفضة.
- الحوالات.
- المصروفات والمسحوبات وشراء الأصل.
- الأرصدة الافتتاحية.
- التحويل والتيفيت.
- التسويات والجرد.

### 7.2 العمليات المحجوبة

- مرتجع الذهب ومرتجع الفضة محجوبان حتى يوجد ربط بالعملية الأصلية وسياسة تكلفة معتمدة.
- فرق جرد الملحقات لا يُرحل حتى تُعتمد حسابات العجز والزيادة الخاصة به.
- أي عملية بلا قاعدة أو بحساب مجهول محجوبة.
- أي عملية تؤدي إلى فشل Phase 5 محجوبة.

### 7.3 قاعدة عدم التوسع

لا يجوز للواجهة عرض “كل الحسابات” كبديل عند غياب القاعدة. غياب القاعدة خطأ إعداد يجب إصلاحه.

---

## 8. تدفقات المستخدم

### 8.1 تسجيل الدخول والتهيئة

1. يدخل المستخدم البريد وكلمة المرور.
2. ينشئ Firebase Auth الجلسة.
3. تُقرأ Claims من الجلسة.
4. تُحمّل إعدادات المستخدم وحساباته وقواعده.
5. إذا كان المستخدم جديدًا، تنفذ تهيئة Seed ذات إصدار واضح مرة واحدة.
6. لا تُعتبر التهيئة ناجحة قبل اكتمال الحسابات والقواعد والتحقق من عددها وبصمتها.
7. عند الفشل، يظهر خطأ ولا تبدأ كتابة قيود.

### 8.2 إنشاء عملية

1. اختيار نوع عملية متاح.
2. اختيار التاريخ والحسابين من القاعدة.
3. إدخال الحقول المطلوبة.
4. حل الحسابات إلى معرفات ثابتة.
5. حساب الأثر على النقد والذهب والفضة والقطع.
6. حساب E21 عند وجود ذهب.
7. محاكاة السجل بعد إضافة العملية في Phase 5.
8. عرض أثر العملية للمراجعة.
9. حفظ القيد وسجل التدقيق كوحدة ذرية.
10. عرض ملخص من نتيجة المحرك نفسها.

### 8.3 تعديل عملية

1. فتح العملية من السجل.
2. تعديل الحقول المسموحة.
3. استبدال العملية القديمة مؤقتًا بالجديدة داخل المحاكاة.
4. رفض التعديل إذا فشلت قواعد الحساب أو التكلفة.
5. حفظ التعديل ولقطة `before/after` ذريًا.

### 8.4 حذف عملية

1. عرض أثر الحذف المتوقع.
2. طلب تأكيد صريح.
3. محاكاة السجل بدون العملية.
4. رفض الحذف إذا فشل المخزون أو التكلفة.
5. تنفيذ الحذف الفردي وتسجيل القيم المحذوفة في سجل التدقيق.

الحذف الجماعي ليس جزءًا من v1.1.

### 8.5 الجرد

1. اختيار حساب مخزون فعال.
2. عرض الرصيد الدفتري ووحدته.
3. إدخال الرصيد الفعلي.
4. حساب الفرق وتصنيفه.
5. حفظ `draft` أو `matched`.
6. للعجز/الزيادة: إعداد قيد وتسعيره والتحقق منه.
7. ترحيل القيد وربطه بورقة الجرد.
8. قفل الورقة بعد `posted` أو `resolved`.

### 8.6 التقارير

1. اختيار تقرير.
2. اختيار فترة أو تاريخ “حتى”.
3. عرض مصدر التقرير وإصدار محركه.
4. منع تقارير التكلفة عند جولة فاشلة.
5. دعم الانتقال من الإجمالي إلى القيود.
6. تصدير التقرير مع معاييره وتاريخ إنشائه.

### 8.7 الأسعار اليومية

1. عرض سعر ووقت آخر تحديث.
2. تعديل المستخدم لسعره الخاص.
3. استخدام السعر في العرض والتقييم السوقي فقط.
4. عدم تغيير COGS أو تكلفة المخزون.

---

## 9. المتطلبات الوظيفية

### 9.1 الهوية والتهيئة

- **FR-01:** يجب استخدام Firebase Auth للمصادقة.
- **FR-02:** يجب اشتقاق صلاحية المدير من Admin Custom Claim فقط.
- **FR-03:** يجب أن تكون تهيئة الحسابات والقواعد idempotent، ذات Version، ولا تنتج Seed جزئيًا أو مكررًا.
- **FR-04:** يجب منع الإدخال حتى اكتمال التهيئة.
- **FR-05:** يجب إظهار حالة الجلسة والاتصال الفعلية، لا مؤشرًا ثابتًا.

### 9.2 الحسابات والقواعد

- **FR-06:** يجب أن يملك كل حساب معرفًا ثابتًا، اسمًا، مجموعة، نوعًا فرعيًا، طبيعة رصيد، وحدة، معدنًا/عيارًا عند الحاجة، وحالة مخزون.
- **FR-07:** يجب حفظ لقطة اسم وتصنيف الحساب داخل القيد لحماية التاريخ من إعادة التسمية.
- **FR-08:** يجب منع حذف حساب مستخدم في قيد؛ يسمح بتعطيله فقط.
- **FR-09:** يجب أن تحدد قاعدة العملية الحسابات والحقول والوحدات المسموحة.
- **FR-10:** يجب أن تفشل العملية عند غياب القاعدة أو معرف الحساب.
- **FR-11:** معرف الحساب والاسم هما المرجع الحالي. لا يجوز اختراع رقم حساب غير موجود.

### 9.3 القيود

- **FR-12:** يجب أن يتضمن كل قيد معرفًا، تاريخ عمل، نوع عملية، حسابين ثابتين، مستخدمًا، ووقت إنشاء.
- **FR-13:** يجب حفظ `operationKind`، و`subTx` للأرصدة الافتتاحية.
- **FR-14:** يجب ألا يُحفظ قيد صفري بلا أثر على أي بُعد.
- **FR-15:** يجب طلب الوزن لأي عملية معدنية.
- **FR-16:** يجب طلب عدد القطع لأي عملية ملحقات.
- **FR-17:** يجب حل العيار من الحساب أو العملية ورفض التعارض.
- **FR-18:** يجب الحفاظ على أرقام المصدر التاريخية منفصلة عن التسلسل الجديد.
- **FR-19:** يجب تشغيل نفس Pipeline للتحقق عند الإنشاء والتعديل والحذف والجرد.
- **FR-20:** يجب حفظ القيد وAudit Event في عملية ذرية أو آلية تعويض مضمونة.
- **FR-21:** يجب منع الحفظ أثناء جولة تكلفة `running` أو `failed`.
- **FR-22:** يجب أن يكون رقم الفاتورة الجديدة فريدًا داخل نطاق المستخدم ونوعها، دون إعادة ترقيم التاريخ.

### 9.4 السجل والطباعة

- **FR-23:** يجب البحث بالتاريخ ونوع العملية والحساب ورقم الفاتورة والملاحظات.
- **FR-24:** يجب التصفية حسب الفترة وبُعد النقد/الذهب/الفضة.
- **FR-25:** يجب دعم Pagination ثابتة لا تفقد نتائج البحث.
- **FR-26:** يجب أن يتضمن التصدير معرفات الحسابات وبيانات المصدر.
- **FR-27:** يجب أن تطبع الفاتورة الحقول التي اختارها المستخدم فقط.
- **FR-28:** يجب ألا تتغير البيانات المحاسبية بتغيير معايرة الطباعة.

### 9.5 الإعدادات

- **FR-29:** يجب حفظ الأسعار والافتتاحيات باسم المالك وتاريخ التحديث.
- **FR-30:** يجب طلب افتتاحية تكلفة سنوية صريحة لأي Dataset لا يطابق البيانات التاريخية المعتمدة.
- **FR-31:** يجب تعطيل مستورد CSV/XLSX العام، الحذف الجماعي، وإعادة الترقيم من واجهة v1.1.
- **FR-32:** يجب عدم عرض تصدير كود المشروع كوظيفة منتج محاسبي.

---

## 10. قواعد العمل

### 10.1 الأبعاد المستقلة

يعمل النظام في أربعة أبعاد:

| البُعد | الوحدة | الاستخدام |
|---|---|---|
| النقد | جنيه، والمحرك الداخلي بالقرش | التدفق والقيمة المالية |
| الذهب | جرام فعلي وE21 | المخزون والالتزام |
| الفضة | جرام فعلي | المخزون والالتزام |
| الملحقات | قطعة | المخزون الكمي |

اسم “Triple Ledger” يشير إلى القوائم الأساسية: Cash وGold وSilver. الملحقات بُعد كمية إضافي ولا تُعامل كمعدن.

لا يُسوّى فرق في بُعد بقيمة من بُعد آخر.

### 10.2 النقد

- البيع أو القبض يزيد الخزنة عندما تكون الخزنة مدينة.
- الشراء أو الدفع أو المصروف أو السحب يخفض الخزنة عندما تكون الخزنة دائنة.
- وجود وزن لا يفترض وجود نقد.
- وجود نقد لا يفترض حركة معدن.

### 10.3 الذهب

- الوزن المدخل بالجرام الفعلي.
- العيارات المدعومة حاليًا: 18 و21 و24.
- معادلة التطبيع:

  `E21 grams = physical grams × karat ÷ 21`

- يخزن محرك التكلفة E21 في سنتيجرام صحيح باستخدام سياسة التقريب المعتمدة في المحرك.
- يبقى الوزن الفعلي محفوظًا بجانب E21.

### 10.4 الفضة

- تُقاس بالجرام الفعلي ولا تتحول إلى E21.
- مخزون الفضة الفعلي منفصل عن التزام تاجر الفضة.
- صافي ملكية الفضة:

  `Physical Silver - Merchant Silver Liability`

### 10.5 الملحقات

- الوحدة هي القطعة.
- لا تحمل حركة ذهب أو فضة.
- الحقل المرجعي للبيانات الجديدة هو `count`.
- `weight` القديم للملحقات يُقرأ عبر طبقة توافق فقط ولا يُكتب كمرجع جديد.
- إذا وُجد `count` و`weight` قديمان بقيمتين مختلفتين، يفشل السجل ويحتاج مراجعة؛ لا يختار النظام قيمة بصمت.

### 10.6 التجار

- استلام معدن من تاجر يزيد المخزون الفعلي والتزام التاجر.
- تسليم معدن للتاجر يخفض المخزون الفعلي والتزام التاجر.
- صافي ملكية المحل = المخزون الفعلي ناقص الالتزام المعدني.
- النقد في استلام التاجر يمثل مصنعية وفق سلوك Phase 5 الحالي.
- في v1.1، تسليم التاجر يخفض الكمية ولا يخفض مجمع التكلفة المتبقي، وفق محرك Phase 5 الحالي.
- إذا أصبحت الكمية صفرًا وبقيت تكلفة، تفشل الجولة ولا تُحفظ العملية.

تغيير قاعدة تكلفة تسليم التاجر يحتاج إلى ADR محاسبي وإصدار جديد؛ لا يتغير ضمن v1.1.

### 10.7 التحويل والتيفيت

- ينقلان الوزن والتكلفة بين مكونات متوافقة.
- لا ينتجان إيرادًا أو ربحًا.
- تُنقل التكلفة الفعلية للمكونات، لا سعر السوق.
- يجب أن يتزن بُعد المعدن.

### 10.8 العجز والزيادة

- العجز يخفض الكمية ويثبت خسارة بقيمة المتوسط المرجح.
- الزيادة تضيف كمية بمتوسط حالي أو آخر متوسط موثق.
- إذا لم توجد تكلفة صالحة، يفشل الترحيل.
- لا يستخدم سعر السوق أو تكلفة صفرية.
- فرق الملحقات محجوب في v1.1 لعدم وجود حساب مقابل معتمد.

### 10.9 الأرصدة الافتتاحية

- يجب تحديد السنة والنوع الفرعي: نقد، ذهب، فضة، ملحقات، تاجر، أو عميل.
- تستخدم الافتتاحيات الموثقة فقط عند تطابق Dataset التاريخي كاملًا وبصمته.
- أي Dataset آخر يحتاج إعداد تكلفة افتتاحية صريح.

### 10.10 الترقيم والمصدر

- التسلسل الجديد لا يستبدل رقم العملية أو الفاتورة التاريخية.
- الأرقام التاريخية لا تُعاد صياغتها لمجرد التكرار أو وجود فجوات.
- معرفات الاستيراد حتمية لمنع التكرار.

### 10.11 بيانات العميل

- الاسم والهاتف اختياريان ولا يؤثران في الحساب.
- لا تُجمع بيانات أخرى في v1.1.
- الهاتف لا يظهر في تقرير أو تصدير عام غير مخصص للعميل.

---

## 11. القواعد المحاسبية

### 11.1 قاعدة الاتزان

كل عملية تُراجع داخل أبعادها المستقلة. لا تجمع جنيهات وجرامات وقطع في ميزان واحد.

### 11.2 البيع

في النموذج المعياري:

- قيمة البيع: مدين نقد/ذمم، دائن إيراد بيع.
- التكلفة: مدين COGS، دائن تكلفة حمل المخزون.
- المعدن: خروج من المخزون إلى تدفق معدن مباع.

Phase 5 يحسب COGS من المتوسط السابق مباشرة للبيع.

### 11.3 الشراء

- القيمة تزيد تكلفة حمل المخزون مقابل انخفاض النقد أو زيادة الالتزام.
- الوزن يزيد المخزون مقابل تدفق معدن مكتسب.
- الشراء ليس مصروف فترة؛ يتحول إلى COGS عند البيع.

### 11.4 المتوسط المرجح المتحرك

لكل مكوّن مخزون:

`WAC = remaining carrying cost ÷ remaining inventory units`

- البيع يستهلك WAC السابق للبيع.
- الشراء يزيد الوحدة والتكلفة بالقيمة المدفوعة.
- التحويل والتيفيت ينقلان التكلفة.
- العجز يخرج تكلفة، والزيادة تحتاج تكلفة موثقة.

### 11.5 الترتيب التاريخي

الترتيب:

1. تاريخ العمل.
2. `seq` الموثوق إن وجد.
3. `createdAt` الموثوق إن وجد.
4. للسجلات القديمة غير المرتبة: سياسة Batch حتمية تضع الافتتاحيات والوارد قبل الصادر.

يجب إنتاج تشخيص عند تطبيق الخطوة 4. لا يُدعى أن هذا هو الترتيب التاريخي الحقيقي.

### 11.6 الإصلاحات التاريخية

يطبق Phase 5 الـOverlays المعتمدة فقط عند تطابق:

- معرف Overlay.
- حالة الاعتماد.
- إصدار السياسة.
- بصمة Dataset.

الـOverlay:

- لا يعدل القيد الأصلي.
- لا ينشئ بيعًا أو شراءً.
- يطبق مرة واحدة.
- يظهر في أثر التدقيق.

### 11.7 قواعد الفشل

تفشل الجولة كاملة عند:

- مخزون غير كافٍ.
- حساب أو Variant غير معروف.
- غياب تكلفة لازمة.
- وحدة غير متوافقة.
- كمية صفر مع تكلفة متبقية.
- تعارض بيانات الملحقات.
- Overlay غير صالح.

لا تُعرض نتائج جزئية ولا يستخدم سعر السوق أو الصفر كبديل.

### 11.8 القوائم المالية

- Legacy Ledger هو مصدر GL وTrial Balance في v1.1.
- Phase 5 هو مصدر Inventory Cost وCOGS وGross Profit.
- تقرير الدخل القديم متعدد الأبعاد هو تقرير حركة تشغيلي، وليس قائمة دخل رسمية.
- Balance Sheet وEquity Statement وFinal Report الحالية ليست قوائم مالية قانونية.
- يجب أن تحمل هذه التقارير تسمية `Operational/Legacy` إلى أن يتم انتقال Canonical.

---

## 12. نموذج البيانات

### 12.1 المجموعات

| المجموعة | الغرض | الملكية |
|---|---|---|
| `users` | الملف الوصفي للمستخدم | المستخدم/المدير حسب القواعد |
| `settings` | الأسعار والافتتاحيات | `userId` |
| `accounts` | الحسابات التشغيلية | `userId` |
| `canonicalAccounts` | كتالوج Shadow | `userId`/admin |
| `entries` | القيود | `userId` |
| `transactionRules` | قواعد التركيبات | `userId` |
| `customRules` | قواعد إضافية | `userId` |
| `inventory_checks` | أوراق الجرد | `userId` |
| `audit_logs` | أثر التدقيق | كتابة مقيدة، قراءة admin |

### 12.2 عقد القيد

| المجموعة | الحقول الجوهرية |
|---|---|
| الهوية | `id`, `seq` |
| المصدر | `legacyOperationNo`, `sourceRow`, `journalNumber`, `sourceRevision` عند وجودها |
| التصنيف | `tx`, `operationKind`, `subTx` |
| الحسابات | `debitAccountId`, `creditAccountId`, أسماء ولقطات الحساب |
| التاريخ | `date`, `createdAt`, `updatedAt` |
| القيم | `cash`, `weight`, `count` |
| المعدن | `karat`, `multiplier`, `gold21Snapshot` |
| المراجع | `invoiceNumber`, `operationNumber`, `inventoryCheckId` |
| البيانات الوصفية | `notes`, `marketPrice`, لقطة الأسعار عند وجودها |
| العميل | `clientName`, `clientPhone` |
| الملكية | `userId` |

### 12.3 ثوابت البيانات

- معرف الحساب هو المرجع؛ الاسم للعرض واللقطة التاريخية.
- `date` هو تاريخ العمل المحلي في `Africa/Cairo`.
- `createdAt` هو وقت تقني للترتيب والتدقيق ولا يستبدل تاريخ العمل.
- القيم التاريخية تبقى بصيغتها الأصلية، وتحول إلى وحدات المحرك عند الحساب.
- البيانات الجديدة للملحقات تستخدم `count`.
- لا يوجد `accountCode` رقمي ملزم في v1.1؛ لا يجوز اشتقاقه من الاسم.
- أي إضافة لـ`entityId` أو `storeId` مؤجلة إلى دعم الفروع.

### 12.4 البيانات المشتقة

لا يحرر المستخدم:

- WAC.
- COGS.
- الربح الإجمالي.
- تكلفة المخزون النهائية.
- E21 المشتق بعد الاعتماد.
- تشخيصات ترتيب التاريخ.

كل نتيجة مشتقة تحمل `engineVersion`, `inputRevision`, `generatedAt`, وحالة الجولة.

### 12.5 النزاهة والتزامن

- كل كتابة تحمل `updatedAt` أو Revision لمنع الكتابة فوق تعديل أحدث.
- التعديل المتزامن يجب أن يفشل برسالة Conflict ويطلب إعادة التحميل.
- الحفظ المتكرر لنفس الطلب يستخدم Idempotency Key.
- لا تُنفذ عملية Entry + Audit على خطوتين مستقلتين غير قابلتين للتعويض.

---

## 13. متطلبات UX

### 13.1 اللغة والوحدات

- العربية وRTL افتراضيان.
- يجب ضمان UTF-8 في العرض والتصدير والطباعة.
- الوحدات المعروضة ثابتة: `ج.م`، `جرام`، `E21`، و`قطعة`.
- الأرقام تستخدم `tabular-nums`.

### 13.2 الهاتف والوصول

- الوظائف الأساسية تعمل دون تمرير أفقي على الهاتف.
- شريط التنقل: الرئيسية، اليومية، عملية، التقارير، المزيد.
- مراعاة Safe Area على iOS.
- كل زر أيقوني يحمل اسم وصول.
- لا تعتمد الحالة على اللون فقط.
- يجب دعم لوحة المفاتيح وتباين مقروء.

### 13.3 نموذج العملية

- الحقول تظهر من تعريف العملية، لا من Regex على الاسم.
- الوحدة تظهر بجوار كل حقل.
- الحسابات غير المسموحة لا تظهر.
- الحقول المطلوبة تحمل علامة ورسالة محددة.
- زر الحفظ يُقفل أثناء الطلب لمنع التكرار.
- لا تضيع المدخلات عند خطأ قابل للتصحيح.

### 13.4 مراجعة الأثر

قبل الحفظ وبعده، يعرض النظام من Posting Result واحد:

1. المبلغ الداخل أو الخارج من الخزنة.
2. وزن الذهب والفضة الداخل أو الخارج.
3. عدد قطع الملحقات الداخل أو الخارج.
4. تغير التزام التاجر.
5. معرف واسم كل حساب؛ ويعرض الكود فقط إذا كان محفوظًا.
6. صافي أثر السيولة والمخزون والملكية.

### 13.5 الحالات

يجب التمييز بين:

- Loading.
- Empty.
- Offline.
- Permission denied.
- Validation failed.
- Cost run running.
- Cost run failed.
- Concurrent edit conflict.

عند فشل التكلفة، تُعرض العملية والحساب والسبب القابل للتنفيذ دون بيانات حساسة.

### 13.6 الشاشات الحساسة

- لا يظهر مستورد عام أو حذف جماعي أو إعادة ترقيم.
- أدوات Canonical تحمل `Preview` ولا تتيح التفعيل.
- “التحليلات المتقدمة” لا تكرر تقرير الربح؛ تُحذف من v1.1 إذا لم تقدم تحليلًا مختلفًا.
- مؤشر الاتصال يعكس الحالة الحقيقية.

---

## 14. التقارير

### 14.1 سجل التقارير

| التقرير | المصدر | الحالة في v1.1 |
|---|---|---|
| اليومية/السجل | `entries` | تشغيلي |
| دفتر الأستاذ | Legacy Ledger | تشغيلي مع شارة `Legacy` |
| ميزان المراجعة | Legacy Ledger | تشغيلي مع شارة `Legacy` |
| حركة الدخل متعددة الأبعاد | Legacy aggregation | `Operational`, غير رسمي |
| المركز المالي | Legacy aggregation | `Operational`, غير رسمي |
| حقوق الملكية | Legacy aggregation | `Operational`, غير رسمي |
| المخزون والتقييم | Phase 5 + أسعار السوق | التكلفة مرجعية؛ السوق تحليلي |
| دورة حياة المخزون | Phase 5 | مرجعي تشغيليًا |
| المبيعات وCOGS والربح | Phase 5 | مرجعي للربح الإجمالي |
| الملخص الشهري | `entries` | تحليلي |
| الكسر والتيفيت | `entries` + Phase 5 عند الحاجة | تحليلي |
| Final Report | مصادر مختلطة | غير رسمي ويجب بيان مصدر كل جزء |
| Canonical reports | Canonical Preview | داخلي/Shadow فقط |

### 14.2 متطلبات مشتركة

- كل تقرير يحدد `from`, `to`, والمنطقة الزمنية.
- تاريخ “حتى” يشمل كامل يوم العمل بتوقيت القاهرة.
- الفلتر المركزي يمر إلى كل أجزاء التقرير.
- كل تقرير يعرض المصدر، الإصدار، تاريخ الإنشاء، وحالة الصلاحية.
- كل إجمالي يدعم Drill-down.
- التصدير يتضمن معايير التقرير.
- فشل Phase 5 يمنع أرقام التكلفة والربح، لا بقية التقارير غير المعتمدة عليه.
- القيمة الدفترية والقيمة السوقية وفرق التقييم غير المحقق تظهر منفصلة.

### 14.3 مؤشرات الرئيسية

- رصيد الخزنة.
- ذهب فعلي بـE21.
- التزام تاجر الذهب وصافي الملكية.
- فضة فعلية.
- التزام تاجر الفضة وصافي الملكية.
- عدد الملحقات.
- حالة وتوقيت آخر جولة تكلفة.
- أسعار السوق ووقت تحديثها.

تستخدم كل بطاقة نفس تاريخ القطع والمصدر المعروضين.

---

## 15. المتطلبات غير الوظيفية

### 15.1 الدقة والحتمية

- الحساب لنفس المدخلات والإصدار ينتج نفس البصمة.
- Golden Baseline الموجود في المستودع هو مرجع القيم؛ لا تنسخ قيمه إلى وثائق قد تصبح قديمة.
- تحديث Golden يحتاج إجراء Owner صريحًا، سببًا، ومراجعة محاسبية.
- الحساب لا يعتمد على نص اسم الحساب.

### 15.2 الأداء

على Dataset المرجعي البالغ 2,169 عملية:

- تظهر الشاشة الأساسية خلال 5 ثوانٍ عند اتصال مستقر وجهاز متوسط.
- تستجيب عمليات البحث والتصفية خلال 500ms بعد اكتمال التحميل.
- تكتمل جولة Phase 5 خلال 5 ثوانٍ على بيئة الاختبار المرجعية.
- إذا تجاوز الحساب 200ms على UI thread، يجب نقله إلى Worker أو خدمة خلفية.
- تستخدم القوائم Pagination ولا تحمل سجلًا غير محدود في DOM.

تُراجع الحدود عند نمو Dataset، ولا تُخفّض دقة الحساب لتحقيق الأداء.

### 15.3 الاعتمادية

- الكتابات الحساسة ذرية أو لها تعويض مضمون.
- الاستيراد الإداري Idempotent وقابل للاستكمال والرجوع.
- توجد نسخة احتياطية يومية بحد أقصى RPO قدره 24 ساعة.
- يجب اختبار استعادة ربع سنوية، وهدف RTO أربع ساعات.
- لا ينفذ حذف جماعي من تطبيق v1.1.

### 15.4 الاتصال

- v1.1 منتج `Online-first`.
- لا يجوز بدء كتابة محاسبية جديدة عندما يعلن المتصفح عدم الاتصال.
- لا تُعرض مزامنة Offline كميزة مضمونة.
- إذا كان Firestore قد صفّ كتابة قبل اكتشاف الانقطاع، تظهر `Pending Sync` ولا تعتبر نهائية حتى تأكيد الخادم.

### 15.5 الصيانة والاختبار

- تعريف واحد لأنواع العمليات.
- Pipeline واحد للكتابة.
- Version صريح لمحرك التكلفة وCanonical وSeed.
- اختبارات Unit لقواعد العمل.
- اختبارات Integration للإنشاء والتعديل والحذف والجرد.
- اختبارات Firestore Rules بالـEmulator.
- اختبارات Golden إلزامية في CI.
- اختبارات RTL والطباعة وسلامة العربية.
- وثائق قديمة موسومة `Historical` أو `Superseded`.

---

## 16. الأمن والخصوصية

### 16.1 المصادقة والتفويض

- Firestore Rules هي سلطة التفويض النهائية.
- Admin Claim فقط يمنح صلاحية الإدارة.
- يجب توحيد Query العميل مع صلاحيات الخادم.
- يجب ألا يحاول العميل إنشاء مستخدم بدور Admin.
- تغيير Claims يتم خارج العميل ومن بيئة موثوقة.

### 16.2 قواعد البيانات

يجب أن تتحقق القواعد أو Backend الموثوق من:

- الحقول المسموحة وأنواعها.
- القيم غير السالبة ضمن معنى العملية.
- ثبات `userId`.
- ملكية الحسابات المستخدمة.
- عدم تعديل جرد مرحل.
- منع الحقول الإدارية من المستخدم العادي.

إذا تعذر فرض قاعدة محاسبية في Firestore Rules، تمر الكتابة عبر Backend موثوق.

### 16.3 التدقيق

يجب تسجيل:

- إنشاء القيد وتعديله وحذفه.
- ترحيل الجرد.
- تغيير الحسابات والقواعد.
- تغيير الافتتاحيات.
- الاستيراد الإداري.
- اعتماد Overlay أو Canonical definition.
- تغيير الصلاحيات.

سجل التدقيق:

- غير قابل للتعديل أو الحذف من العميل.
- يحتوي المستخدم والوقت والكيان والإجراء و`before/after` أو المرجع الآمن لهما.
- لا يحتوي أسرارًا أو بيانات عميل غير لازمة.

### 16.4 البيانات الشخصية

- تقتصر بيانات العميل على الاسم والهاتف الاختياريين.
- الوصول لهما حسب ملكية القيد.
- لا يظهر الهاتف في Log أو رسالة خطأ.
- تصدير بيانات العميل إجراء صريح، لا جزء من تقارير عامة.
- Golden Data وFixtures معقمة.

### 16.5 العمليات الحساسة

- الاستيراد التاريخي يتم بأداة إدارية مع Preview وManifest وRollback.
- الحذف الجماعي وإعادة الترقيم وتصدير كود المشروع غير متاحة في واجهة v1.1.
- الأسرار وTokens لا تُحفظ في المستودع أو Local Storage.

---

## 17. نطاق MVP

### 17.1 داخل MVP

- المصادقة ودورا `user/admin`.
- تهيئة الحسابات والقواعد.
- العمليات المدعومة بقواعد صالحة، عدا المحجوب منها.
- إنشاء وتعديل وحذف فردي مع Preflight.
- Cash/Gold/Silver/Accessory tracking.
- E21 للذهب.
- المخزون الفعلي والتزام التاجر وصافي الملكية.
- Phase 5 للتكلفة وCOGS والربح الإجمالي.
- الجرد للذهب والفضة، ومطابقة الملحقات دون ترحيل فرقها.
- اليومية والسجل والبحث.
- GL وTrial Balance مع شارة Legacy.
- تقارير المخزون والربح من Phase 5.
- تقييم سوقي منفصل.
- تصدير قراءة وطباعة فاتورة.
- Audit كامل.
- Online-first وحالة اتصال واضحة.

### 17.2 خارج MVP

- المرتجعات.
- ترحيل فرق الملحقات.
- Canonical production.
- قوائم مالية رسمية.
- استيراد واجهة عام.
- حذف جماعي وإعادة ترقيم.
- Offline كامل.
- أدوار إضافية.
- فروع وكيانات متعددة.
- ضرائب وفاتورة إلكترونية.

---

## 18. الحالات الحدية الإلزامية

يجب اختبار ورفض أو معالجة الحالات التالية بوضوح:

### 18.1 المدخلات

- مبلغ/وزن/عدد فارغ، صفر بلا أثر، سالب، أو غير رقمي.
- تعارض عيار الحساب مع عيار العملية.
- ذهب بلا عيار أو فضة تحمل عيارًا.
- ملحق له `count` و`weight` تاريخيان مختلفان.
- دقة أكبر من الوحدة الداخلية.

### 18.2 المخزون والتكلفة

- بيع قبل افتتاحية أو وارد.
- بيع أكبر من الرصيد بفارق تقريبي.
- زيادة بلا WAC موثق.
- تحويل بين وحدتين أو معدنين مختلفين.
- حذف شراء تعتمد عليه مبيعات لاحقة.
- تعديل تاريخي يغير كل التكلفة اللاحقة.
- تسليم تاجر يصل بالصفر مع تكلفة متبقية.
- Overlay مكرر أو ببصمة مختلفة.

### 18.3 الحسابات والتاريخ

- حساب معطل مستخدم تاريخيًا.
- اسمان متطابقان لمعرفين مختلفين.
- اسم لقطة لا يطابق الاسم الحالي.
- عملية بلا قاعدة.
- سجل بلا `seq` و`createdAt`.
- أرقام تاريخية مكررة.

### 18.4 التزامن والاتصال

- ضغط حفظ مرتين.
- تعديل من جهازين.
- انقطاع بين Preflight والكتابة.
- جلسة منتهية أثناء Batch.
- إعادة تحميل أثناء جولة التكلفة.
- كتابة معلقة لم يؤكدها الخادم.

### 18.5 التواريخ

- تاريخ مستقبلي.
- سنة بلا تكلفة افتتاحية.
- عملية عند منتصف الليل.
- فرق بين تاريخ العمل ووقت الإنشاء.
- تقرير ينتهي في آخر اليوم بتوقيت القاهرة.

---

## 19. Release Blockers

لا يُعتبر التطبيق مستوفيًا لـPRD v1.1 قبل إغلاق الآتي:

| ID | الفجوة الحالية | شرط الإغلاق |
|---|---|---|
| RB-01 | التعديل والحذف قد يكتبان قبل اكتمال تحقق التكلفة | Preflight ناجح قبل أي كتابة |
| RB-02 | صلاحية Admin في العميل تعتمد جزئيًا على البريد | الاعتماد الكامل على Custom Claim |
| RB-03 | إنشاء ملف المستخدم قد يطلب دور Admin من العميل | Provisioning موثوق وعدم رفع الدور من العميل |
| RB-04 | مستورد الإعدادات غير آمن ولا يطابق التصدير | إزالته/تعطيله من v1.1 |
| RB-05 | الحذف الجماعي وإعادة الترقيم متاحان من الإعدادات | إزالتهما/تعطيلهما |
| RB-06 | فرق جرد الملحقات يستخدم حسابات الذهب | منع ترحيل فرق الملحقات |
| RB-07 | `subTx` الافتتاحي لا يُحفظ دائمًا | حفظه والتحقق منه |
| RB-08 | ملخص القيد يعتمد على النص ولا يغطي كل الأبعاد | توليده من Posting Result |
| RB-09 | قاعدة مفقودة قد تفتح كل الحسابات | Fail-closed |
| RB-10 | فحص رصيد البيع يعتمد على بادئة اسم | استخدام ID وmetadata |
| RB-11 | Audit غير شامل لكل الكتابات الحساسة | تغطية أحداث القسم 16.3 |
| RB-12 | Firestore Rules لا تتحقق من عقد القيد بما يكفي | Schema validation أو Backend موثوق |
| RB-13 | حذف الحساب بلا Referential Guard واضح | تعطيل الحساب المستخدم بدل حذفه |
| RB-14 | تقارير مختلفة بلا تسمية مصدر واضحة | تطبيق Report Registry وشارات المصدر |
| RB-15 | “Advanced Analytics” يكرر الربح | حذف الخيار المكرر |
| RB-16 | Offline/Pending writes غير واضحة | منع الكتابة Offline وإظهار Pending Sync |
| RB-17 | Seed يكتب تلقائيًا عند فراغ الاستعلام | Bootstrap idempotent ومتحقق |
| RB-18 | تصدير الكود في الواجهة لا يطابق مصادقة الخادم | إزالته من المنتج |

---

## 20. معايير القبول النهائية

### 20.1 القيود

- **AC-01:** البيع الصحيح يغير النقد/الذمم والوزن وCOGS كما تحدد القاعدة والمحرك.
- **AC-02:** الشراء الصحيح يزيد المخزون وتكلفة الحمل ولا يسجل شراءً كمصروف فترة.
- **AC-03:** عملية فضة لا تغير الذهب، وعملية ملحقات لا تغير معدنًا.
- **AC-04:** لا يُحفظ قيد بلا أثر أو وزن معدني مطلوب أو معرف حساب.
- **AC-05:** لا يُحفظ إنشاء أو تعديل أو حذف يفشل Phase 5.
- **AC-06:** يحفظ القيد `operationKind` و`subTx` المطلوبين.
- **AC-07:** Entry وAudit Event ينجحان معًا أو يفشلان معًا.
- **AC-08:** إعادة نفس طلب الحفظ لا تنشئ قيدًا ثانيًا.

### 20.2 التكلفة

- **AC-09:** Golden Tests تطابق Baseline المعتمد وبصمته.
- **AC-10:** لا ينتج التحويل أو التيفيت ربحًا.
- **AC-11:** المخزون غير الكافي يفشل الجولة دون نتيجة جزئية.
- **AC-12:** لا يستخدم سعر السوق أو تكلفة صفرية بديلة.
- **AC-13:** يطبق Overlay معتمد مرة واحدة فقط على Dataset المطابق.
- **AC-14:** تشخيص الترتيب يظهر للسجلات التي استخدمت Batch fallback.

### 20.3 الجرد

- **AC-15:** الجرد المطابق لا ينشئ قيدًا.
- **AC-16:** جرد الذهب أو الفضة المختلف لا يُرحل قبل تحقق التكلفة.
- **AC-17:** فرق ملحقات لا يمكن ترحيله في v1.1.
- **AC-18:** الجرد المرحل غير قابل للتعديل أو الحذف ومرتبط بقيده.

### 20.4 التقارير

- **AC-19:** رصيد الخزنة في الرئيسية يطابق Legacy Ledger لنفس تاريخ القطع.
- **AC-20:** تكلفة المخزون والربح يطابقان Phase 5 لنفس `inputRevision`.
- **AC-21:** كل تقرير يعرض المصدر والفترة والإصدار والحالة.
- **AC-22:** Legacy financial reports تحمل `Operational/Not statutory`.
- **AC-23:** القيمة السوقية لا تغير القيمة الدفترية أو COGS.
- **AC-24:** يمكن تتبع إجمالي التقرير إلى القيود.

### 20.5 الأمن

- **AC-25:** مستخدم عادي لا يقرأ أو يكتب بيانات مستخدم آخر.
- **AC-26:** البريد وحده لا يمنح Admin access.
- **AC-27:** العميل لا يستطيع رفع دوره أو تعديل Audit Log.
- **AC-28:** الجرد المرحل محمي بقواعد الخادم.
- **AC-29:** كتابة بقيم أو وحدات أو `userId` غير صالح تُرفض.
- **AC-30:** لا توجد أدوات حذف جماعي أو استيراد عام أو تصدير كود في واجهة v1.1.

### 20.6 UX والاعتمادية

- **AC-31:** ملخص العملية يعرض الأبعاد والحسابات من نتيجة مركزية واحدة.
- **AC-32:** كل حالة تحميل/فشل/Offline/Conflict لها عرض مستقل.
- **AC-33:** النص العربي سليم في الواجهة والطباعة والتصدير.
- **AC-34:** حدود الأداء في القسم 15.2 تمر على بيئة الاختبار المرجعية.
- **AC-35:** النسخة الاحتياطية والاستعادة التجريبية ناجحتان قبل الإطلاق.

---

## 21. النطاق المستقبلي

يحتاج كل بند إلى PRD/ADR مستقل:

- انتقال Legacy → Canonical مع Parallel Run ومصالحة.
- قوائم مالية رسمية.
- إغلاق فترات وسنوات وAnnual Cost Snapshots.
- مرتجعات مرتبطة بالأصل.
- حسابات عجز/زيادة الملحقات.
- أدوار Operator/Auditor.
- فروع وكيانات متعددة.
- Import UI آمن مع Mapping وPreview وRollback.
- Offline conflict resolution.
- أسعار آلية موثقة.
- الضرائب والفاتورة الإلكترونية.
- موافقات مزدوجة للتسويات الكبيرة.

---

## 22. القرارات المؤجلة غير المعطلة لـv1.1

هذه البنود لا تخلق غموضًا في MVP لأن الوظيفة المرتبطة بها خارج النطاق أو محجوبة:

| القرار المؤجل | قاعدة v1.1 الآمنة |
|---|---|
| سياسة المرتجعات | المرتجعات محجوبة |
| حسابات فرق الملحقات | الترحيل محجوب |
| Canonical Cutover | Legacy يبقى إنتاجيًا وCanonical Preview |
| أدوار الموظف والمراجع | `user/admin` فقط |
| الفروع | كيان واحد لكل ملكية مستخدم حالية |
| الإغلاق السنوي | لا توجد وظيفة إغلاق في v1.1 |
| الضرائب | لا توجد تقارير ضريبية |
| Offline | Online-first ومنع الكتابة الجديدة Offline |
| أكواد الحسابات الرقمية | ID + Name فقط؛ لا اختراع كود |
| إعادة تصميم تكلفة تسليم التاجر | سلوك Phase 5 الحالي ثابت في v1.1 |

---

## 23. خط الأساس المراجع

- التطبيق React/Vite مع Firebase Auth وFirestore وZustand.
- البيانات التاريخية المعتمدة تحتوي 2,169 عملية.
- Phase 4 يطابق 2,168 عملية، مع عملية واحدة `Legacy-only`.
- Legacy Ledger متوازن في خط الأساس الموثق.
- Phase 5 لديه Golden Dataset معقم وBaseline معتمد.
- توجد Overlays تاريخية معتمدة تُطبق دون تعديل القيود الأصلية.
- المخزون الفعلي والتزام التاجر وصافي الملكية مقاييس منفصلة.
- وثائق الاستيراد القديمة وتقارير الأعطال التاريخية لا تتقدم على الاختبارات والنتائج الأحدث.

قيم Golden التفصيلية لا تُكرر هنا؛ الملف المعتمد في المستودع هو المرجع لمنع تقادم الأرقام داخل الوثائق.

---

## 24. إدارة التغيير

بعد تجميد v1.1:

- لا تُغير قاعدة محاسبية دون ADR يراجعه مالك القرار المحاسبي.
- لا تُضاف عملية أو وحدة أو دور دون تحديث هذه الوثيقة والاختبارات.
- أي تغيير في Golden Baseline يحتاج سببًا، Diff، وموافقة صريحة.
- إصلاح Bug يعيد التنفيذ إلى هذه الوثيقة لا يحتاج تغيير PRD.
- اختلاف التنفيذ عن هذه الوثيقة يُسجل كفجوة، ولا يُعدل النص لتبرير السلوك تلقائيًا.
- الإصدار التالي يكون v1.2 للتوضيح المتوافق، أو v2.0 لتغيير قاعدة/نطاق جوهري.

---

## 25. تعريف الجاهزية

تكون متطلبات v1.1 جاهزة للتسليم لأنها:

- تحدد مصدر الحقيقة لكل رقم.
- تفصل الوضع الحالي عن النطاق المستقبلي.
- تحجب السياسات المحاسبية غير المعتمدة بدل اختراعها.
- تحدد دورين وصلاحياتهما.
- تزيل الاستيراد والحذف الجماعي وإعادة الترقيم من MVP.
- تحدد عقد القيد ووحداته وحالات فشله.
- تحدد Release Blockers ومعايير قبول قابلة للاختبار.

ويكون **التطبيق** جاهزًا للإنتاج فقط بعد:

1. إغلاق جميع Release Blockers.
2. نجاح AC-01 إلى AC-35.
3. نجاح Golden وFirestore Rules وIntegration وRTL tests.
4. نجاح Backup/Restore rehearsal.
5. توقيع صاحب المنتج والمحاسب المعتمد على نتائج القبول.
